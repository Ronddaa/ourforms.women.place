export const SORT_ORDER = {
  ASC: "asc",
  DESC: "desc",
};

export const SMTP = {
  SMTP_HOST: "SMTP_HOST",
  SMTP_PORT: "SMTP_PORT",
  SMTP_USER: "SMTP_USER",
  SMTP_PASS: "SMTP_PASS",
  SMTP_FROM: "SMTP_FROM",
};

export const ONE_DAY = 24 * 60 * 60 * 1000;
export const ONE_WEEK = 24 * 60 * 60 * 1000 * 7;
